/**
 * Converts `set` to its value-value pairs.
 *
 * @private
 * @param {Object} set The set to convert.
 * @returns {Array} Returns the value-value pairs.
 */

var _setToPairs;
var hasRequired_setToPairs;

function require_setToPairs () {
	if (hasRequired_setToPairs) return _setToPairs;
	hasRequired_setToPairs = 1;
	function setToPairs(set) {
	  var index = -1,
	      result = Array(set.size);

	  set.forEach(function(value) {
	    result[++index] = [value, value];
	  });
	  return result;
	}

	_setToPairs = setToPairs;
	return _setToPairs;
}

export { require_setToPairs as __require };
