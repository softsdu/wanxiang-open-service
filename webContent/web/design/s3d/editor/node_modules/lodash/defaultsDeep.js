import { __require as require_apply } from './_apply.js';
import { __require as require_baseRest } from './_baseRest.js';
import { __require as require_customDefaultsMerge } from './_customDefaultsMerge.js';
import { __require as requireMergeWith } from './mergeWith.js';

var defaultsDeep_1;
var hasRequiredDefaultsDeep;

function requireDefaultsDeep () {
	if (hasRequiredDefaultsDeep) return defaultsDeep_1;
	hasRequiredDefaultsDeep = 1;
	var apply = require_apply(),
	    baseRest = require_baseRest(),
	    customDefaultsMerge = require_customDefaultsMerge(),
	    mergeWith = requireMergeWith();

	/**
	 * This method is like `_.defaults` except that it recursively assigns
	 * default properties.
	 *
	 * **Note:** This method mutates `object`.
	 *
	 * @static
	 * @memberOf _
	 * @since 3.10.0
	 * @category Object
	 * @param {Object} object The destination object.
	 * @param {...Object} [sources] The source objects.
	 * @returns {Object} Returns `object`.
	 * @see _.defaults
	 * @example
	 *
	 * _.defaultsDeep({ 'a': { 'b': 2 } }, { 'a': { 'b': 1, 'c': 3 } });
	 * // => { 'a': { 'b': 2, 'c': 3 } }
	 */
	var defaultsDeep = baseRest(function(args) {
	  args.push(undefined, customDefaultsMerge);
	  return apply(mergeWith, undefined, args);
	});

	defaultsDeep_1 = defaultsDeep;
	return defaultsDeep_1;
}

export { requireDefaultsDeep as __require };
