import { __require as require_baseForRight } from './_baseForRight.js';
import { __require as require_castFunction } from './_castFunction.js';
import { __require as requireKeysIn } from './keysIn.js';

var forInRight_1;
var hasRequiredForInRight;

function requireForInRight () {
	if (hasRequiredForInRight) return forInRight_1;
	hasRequiredForInRight = 1;
	var baseForRight = require_baseForRight(),
	    castFunction = require_castFunction(),
	    keysIn = requireKeysIn();

	/**
	 * This method is like `_.forIn` except that it iterates over properties of
	 * `object` in the opposite order.
	 *
	 * @static
	 * @memberOf _
	 * @since 2.0.0
	 * @category Object
	 * @param {Object} object The object to iterate over.
	 * @param {Function} [iteratee=_.identity] The function invoked per iteration.
	 * @returns {Object} Returns `object`.
	 * @see _.forIn
	 * @example
	 *
	 * function Foo() {
	 *   this.a = 1;
	 *   this.b = 2;
	 * }
	 *
	 * Foo.prototype.c = 3;
	 *
	 * _.forInRight(new Foo, function(value, key) {
	 *   console.log(key);
	 * });
	 * // => Logs 'c', 'b', then 'a' assuming `_.forIn` logs 'a', 'b', then 'c'.
	 */
	function forInRight(object, iteratee) {
	  return object == null
	    ? object
	    : baseForRight(object, castFunction(iteratee), keysIn);
	}

	forInRight_1 = forInRight;
	return forInRight_1;
}

export { requireForInRight as __require };
