import { __require as require_baseFunctions } from './_baseFunctions.js';
import { __require as requireKeysIn } from './keysIn.js';

var functionsIn_1;
var hasRequiredFunctionsIn;

function requireFunctionsIn () {
	if (hasRequiredFunctionsIn) return functionsIn_1;
	hasRequiredFunctionsIn = 1;
	var baseFunctions = require_baseFunctions(),
	    keysIn = requireKeysIn();

	/**
	 * Creates an array of function property names from own and inherited
	 * enumerable properties of `object`.
	 *
	 * @static
	 * @memberOf _
	 * @since 4.0.0
	 * @category Object
	 * @param {Object} object The object to inspect.
	 * @returns {Array} Returns the function names.
	 * @see _.functions
	 * @example
	 *
	 * function Foo() {
	 *   this.a = _.constant('a');
	 *   this.b = _.constant('b');
	 * }
	 *
	 * Foo.prototype.c = _.constant('c');
	 *
	 * _.functionsIn(new Foo);
	 * // => ['a', 'b', 'c']
	 */
	function functionsIn(object) {
	  return object == null ? [] : baseFunctions(object, keysIn(object));
	}

	functionsIn_1 = functionsIn;
	return functionsIn_1;
}

export { requireFunctionsIn as __require };
