import { __require as requireToPairs } from './toPairs.js';

var entries;
var hasRequiredEntries;

function requireEntries () {
	if (hasRequiredEntries) return entries;
	hasRequiredEntries = 1;
	entries = requireToPairs();
	return entries;
}

export { requireEntries as __require };
