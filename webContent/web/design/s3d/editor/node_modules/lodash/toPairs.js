import { __require as require_createToPairs } from './_createToPairs.js';
import { __require as requireKeys } from './keys.js';

var toPairs_1;
var hasRequiredToPairs;

function requireToPairs () {
	if (hasRequiredToPairs) return toPairs_1;
	hasRequiredToPairs = 1;
	var createToPairs = require_createToPairs(),
	    keys = requireKeys();

	/**
	 * Creates an array of own enumerable string keyed-value pairs for `object`
	 * which can be consumed by `_.fromPairs`. If `object` is a map or set, its
	 * entries are returned.
	 *
	 * @static
	 * @memberOf _
	 * @since 4.0.0
	 * @alias entries
	 * @category Object
	 * @param {Object} object The object to query.
	 * @returns {Array} Returns the key-value pairs.
	 * @example
	 *
	 * function Foo() {
	 *   this.a = 1;
	 *   this.b = 2;
	 * }
	 *
	 * Foo.prototype.c = 3;
	 *
	 * _.toPairs(new Foo);
	 * // => [['a', 1], ['b', 2]] (iteration order is not guaranteed)
	 */
	var toPairs = createToPairs(keys);

	toPairs_1 = toPairs;
	return toPairs_1;
}

export { requireToPairs as __require };
