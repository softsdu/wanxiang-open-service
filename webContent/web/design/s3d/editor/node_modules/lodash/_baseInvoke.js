import { __require as require_apply } from './_apply.js';
import { __require as require_castPath } from './_castPath.js';
import { __require as requireLast } from './last.js';
import { __require as require_parent } from './_parent.js';
import { __require as require_toKey } from './_toKey.js';

var _baseInvoke;
var hasRequired_baseInvoke;

function require_baseInvoke () {
	if (hasRequired_baseInvoke) return _baseInvoke;
	hasRequired_baseInvoke = 1;
	var apply = require_apply(),
	    castPath = require_castPath(),
	    last = requireLast(),
	    parent = require_parent(),
	    toKey = require_toKey();

	/**
	 * The base implementation of `_.invoke` without support for individual
	 * method arguments.
	 *
	 * @private
	 * @param {Object} object The object to query.
	 * @param {Array|string} path The path of the method to invoke.
	 * @param {Array} args The arguments to invoke the method with.
	 * @returns {*} Returns the result of the invoked method.
	 */
	function baseInvoke(object, path, args) {
	  path = castPath(path, object);
	  object = parent(object, path);
	  var func = object == null ? object : object[toKey(last(path))];
	  return func == null ? undefined : apply(func, object, args);
	}

	_baseInvoke = baseInvoke;
	return _baseInvoke;
}

export { require_baseInvoke as __require };
