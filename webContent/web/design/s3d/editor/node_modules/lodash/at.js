import { __require as require_baseAt } from './_baseAt.js';
import { __require as require_flatRest } from './_flatRest.js';

var at_1;
var hasRequiredAt;

function requireAt () {
	if (hasRequiredAt) return at_1;
	hasRequiredAt = 1;
	var baseAt = require_baseAt(),
	    flatRest = require_flatRest();

	/**
	 * Creates an array of values corresponding to `paths` of `object`.
	 *
	 * @static
	 * @memberOf _
	 * @since 1.0.0
	 * @category Object
	 * @param {Object} object The object to iterate over.
	 * @param {...(string|string[])} [paths] The property paths to pick.
	 * @returns {Array} Returns the picked values.
	 * @example
	 *
	 * var object = { 'a': [{ 'b': { 'c': 3 } }, 4] };
	 *
	 * _.at(object, ['a[0].b.c', 'a[1]']);
	 * // => [3, 4]
	 */
	var at = flatRest(baseAt);

	at_1 = at;
	return at_1;
}

export { requireAt as __require };
