import { __require as require_baseGet } from './_baseGet.js';
import { __require as require_baseSet } from './_baseSet.js';

var _baseUpdate;
var hasRequired_baseUpdate;

function require_baseUpdate () {
	if (hasRequired_baseUpdate) return _baseUpdate;
	hasRequired_baseUpdate = 1;
	var baseGet = require_baseGet(),
	    baseSet = require_baseSet();

	/**
	 * The base implementation of `_.update`.
	 *
	 * @private
	 * @param {Object} object The object to modify.
	 * @param {Array|string} path The path of the property to update.
	 * @param {Function} updater The function to produce the updated value.
	 * @param {Function} [customizer] The function to customize path creation.
	 * @returns {Object} Returns `object`.
	 */
	function baseUpdate(object, path, updater, customizer) {
	  return baseSet(object, path, updater(baseGet(object, path)), customizer);
	}

	_baseUpdate = baseUpdate;
	return _baseUpdate;
}

export { require_baseUpdate as __require };
