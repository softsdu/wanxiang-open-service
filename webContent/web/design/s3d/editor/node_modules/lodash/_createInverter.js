import { __require as require_baseInverter } from './_baseInverter.js';

var _createInverter;
var hasRequired_createInverter;

function require_createInverter () {
	if (hasRequired_createInverter) return _createInverter;
	hasRequired_createInverter = 1;
	var baseInverter = require_baseInverter();

	/**
	 * Creates a function like `_.invertBy`.
	 *
	 * @private
	 * @param {Function} setter The function to set accumulator values.
	 * @param {Function} toIteratee The function to resolve iteratees.
	 * @returns {Function} Returns the new inverter function.
	 */
	function createInverter(setter, toIteratee) {
	  return function(object, iteratee) {
	    return baseInverter(object, setter, toIteratee(iteratee), {});
	  };
	}

	_createInverter = createInverter;
	return _createInverter;
}

export { require_createInverter as __require };
