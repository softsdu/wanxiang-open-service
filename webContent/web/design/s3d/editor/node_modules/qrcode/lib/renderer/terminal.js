import { __exports as terminal } from '../../../../_virtual/terminal.js';
import { __require as requireTerminal$1 } from './terminal/terminal.js';
import { __require as requireTerminalSmall } from './terminal/terminal-small.js';

var hasRequiredTerminal;

function requireTerminal () {
	if (hasRequiredTerminal) return terminal;
	hasRequiredTerminal = 1;
	const big = requireTerminal$1();
	const small = requireTerminalSmall();

	terminal.render = function (qrData, options, cb) {
	  if (options && options.small) {
	    return small.render(qrData, options, cb)
	  }
	  return big.render(qrData, options, cb)
	};
	return terminal;
}

export { requireTerminal as __require };
